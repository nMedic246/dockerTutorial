CREATE TABLE tutorials (
                           id SERIAL PRIMARY KEY,
                           title VARCHAR(100),
                           description TEXT,
                           published BOOLEAN
);